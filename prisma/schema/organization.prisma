model Organization {
    id String @id @default(uuid())

    name String

    slug String @unique

    memberships Membership[]

    roles Role[]

    courses Course[]

    createdAt DateTime @default(now())
}
